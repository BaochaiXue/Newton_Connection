# Implement: data_collection_protocol

## Preconditions

- confirm the downstream consumers of new captures
- identify the minimum capture constraints that materially affect reconstruction

## Canonical Commands

There is no canonical script yet; this task is protocol-definition work.

## Step Sequence

1. define the sequence structure
2. define the save list and naming requirements
3. write the protocol so it can be handed to a student directly

## Validation

- downstream consumers are explicitly covered
- no hidden oral assumptions remain

## Output Paths

- protocol docs under `docs/bridge/tasks/` and any linked runbooks
