# Robot Sphere Inflation Root-Cause Board

Last updated: 2026-04-04

| Hypothesis | Why it would force larger spheres | Evidence for | Evidence against | Instrumentation needed | Disambiguating experiment | Likely honest fix |
| --- | --- | --- | --- | --- | --- | --- |
| H1 `contact reference wrong` | Bigger spheres can compensate if the commanded point is not on the true contact-capable finger surface | **Primary.** In c12 the target stays on gripper-center semantics; mean target -> nearest finger-box surface distance is `0.0424 m`, and first-contact gripper-center -> actual contact surface distance is `0.0780 m`. | None of the accepted evidence suggests the commanded reference is already fingertip/finger-pad aligned. | complete | completed from current saved histories | use finger-surface-aware geometry or explicit honest contactor |
| H2 `Z-stack stale` | Bigger spheres can catch the rope if the finger rides above the real rope top/contact line | Secondary. c12 settled active-surface minus local-rope-top is about `0.0538 m`; c19 drops to `0.0347 m`. | Not primary: c15/c16/c19/c20 changed contact timing a lot without meaningful parser-level clearance changes. | complete | completed from current saved histories | recompute true contact surface vs local rope-top; do not rely on stale mental model |
| H3 `XY path miss` | Bigger spheres can catch lateral near-miss paths | **Co-primary.** True-size first contact shifts from `4.3355 s` (c15) to `1.23395 s` (c19) when laydown/preroll/base move the real finger path onto the real rope laydown. | None of the direct-finger evidence supports “sphere size alone” as the main reason c19 works better. | complete | completed from existing c15/c16/c19/c20 matrix | retune laydown/base/path instead of enlarging proxies |
| H4 `rope thinner than old mental model` | If the true rope is thinner than the old visual/mental model, precision demands rise and spheres look necessary | Secondary. c12 used `~0.026 m` rope radius; true-size runs and c08 use `~0.0026 m`. | Not sufficient by itself: c19 still reaches the thin rope early once laydown/base alignment is corrected. | complete | completed by c12 vs true-size vs c08 comparison | update geometry assumptions; keep render/physical rope thickness honest |
| H5 `proxy semantics misleading` | Bigger proxy spheres may only affect diagnostics/acceptance, not true contact | Secondary. Proxy sweep on fixed histories changes proxy onset while actual finger-box onset and rope motion do not. | In promoted c12, actual finger-box contact still precedes `finger_span`; proxies are no longer the accepted proof surface. | complete | completed as offline sphere sweep on fixed histories | keep proxies diagnostic-only; do not let them certify direct contact |
| H6 `controller masks geometry error` | Kinematic/controller error can be hidden by bigger spheres even if physics is not improved | Secondary. A controller-driven path can remain slightly wrong while a large proxy still “catches” the rope in diagnostics. | The visible-tool control shows the same tabletop context can be hit honestly without sphere fiction, so controller semantics are not the primary sphere explanation. | complete | completed via direct-finger vs visible-tool comparison plus physical-blocking status | keep this as a separate blocked stronger-task concern |
| H7 `combination` | More than one mismatch can make spheres look necessary | **True overall.** The ranked report lands on H1 + H3 primary, with H4/H2/H5/H6 secondary. | no real evidence against combination | complete | completed | combined honest fix, not bigger spheres |
