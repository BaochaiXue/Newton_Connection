# True-Size Rope Recalibration Diagnosis Board

Last updated: 2026-04-03

| Suspected coupled factor | Evidence | Confidence | How to measure it | Candidate fix |
| --- | --- | --- | --- | --- |
| stale tabletop_curve shape | confirmed by c15/c16 first frames and laydown audit: fixed `arch=0.016`, `s_curve=0.028` over-shape the `0.1x` rope and expose kinky / hand-forced laydown | high | compare first visible frames and preroll-rested laydown under `tabletop_curve` vs `tabletop_shallow_curve` | replace the old default for true-size exploration with `tabletop_shallow_curve` and softer/longer preroll |
| insufficient hidden preroll settle | c15/c16 settle early mainly because damping freezes a bad laydown; c18 softer preroll improved the starting shape | medium | compare preroll metrics, first visible frame, and residual support-patch speed | keep preroll around `3.0-4.0s` and damping around `2.0-3.0` while evaluating laydown |
| stale tabletop-rope-height | the accepted tabletop placement was tuned around the thicker rope and old laydown; new rope top sits materially lower | high | compare settled rope top / COM against c12 and new candidates | only adjust after laydown is natural; do not trust old c12 semantics blindly |
| stale tabletop-ee-offset-z | lower-level tabletop Z semantics are stale, but under `joint_trajectory` this is not the active control surface | medium | compare IK path vs joint path and actual finger-box approach heights | keep documented, but do not spend the next search budget here unless switching back to IK |
| stale contact/push clearances | world-space clearance numbers are stale for the smaller rope, but they mostly affect overlays/diagnostics on the accepted joint path | low | verify whether changing them alters actual joint-path contact timing | defer until the controller path changes |
| stale XY push offsets | subagent audit says the finger now scrapes the rope from a stale lateral line; c18 shows improvement but still not a central early hit | high | compare first-contact frames and finger-box/rope geometry against c12 | bounded XY/base sweep; if that saturates, add explicit joint-waypoint recalibration |
| stale robot base offset | confirmed contributor: `c18` base change plus shallow curve moved contact from `4.3355s` to `3.2683s` | high | compare first-contact timing and contact patch placement across base-offset variants | continue bounded base-pose search before rewriting the controller |
| render-only / physical mismatch | no longer the active regression in the new true-size branch because visual radius follows physical radius again | low | inspect args and render defaults | keep visual radius honest; do not use render-only rescue knobs |
| actual finger-box contact happening too late | hard evidence: c12 `1.6675s`, c15 `4.3355s`, c16 `3.63515s`, c17 `3.56845s`, c18 `3.2683s` | high | compare summary contact onset times and full-video review | use this as the top scalar score for bounded search, but keep visual review fail-closed |
| camera making a true near-touch look like a miss | secondary only; c15/c16 fail primarily because contact is actually late/weak, not just because of framing | low | compare hero and validation after physics improves | postpone camera tightening until reachability is solved |
