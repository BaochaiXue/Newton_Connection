# Native Robot Physical Blocking Minimal Board

Last updated: 2026-04-04

| Hypothesis | Evidence | Why this breaks real blocking | Bridge/demo fix possible? | Minimal native fix | Validation method |
| --- | --- | --- | --- | --- | --- |
| H1 `current bridge path still overwrites robot state` | Existing stronger-task reports already show `joint_trajectory` does direct `state_in/state_out` overwrites. | Contact-generated tracking error cannot persist. | Not for the old path; must avoid overwrite. | Use a non-overwrite actuation path for Stage 0. | control update-order audit, target-vs-actual report |
| H2 `SemiImplicit joint-target path exists but is unstable or misused` | Current core code reads `control.joint_target_pos/vel`, `model.joint_target_ke/kd`, and `control.joint_f`; local smoke on the Franka build produced `NaN` body motion under a large target jump. | The path may exist but still fail to give a stable, useful articulated actuator for blocking. | Possibly yes. Need bounded re-check before touching core. | Stage-0 micro-benchmark plus smaller target / gain audit. | isolated no-rope body-motion smokes, Stage-0 blocking bundle |
| H3 `body motion exists but previous proof watched the wrong state surface` | `SolverSemiImplicit` integrates `body_q/body_qd`; `joint_q` is not the right truth surface for maximal-coordinate body motion. | A false “no motion” conclusion could send work into unnecessary core edits. | Yes, if Stage-0 body-level motion is measurable. | Rebase diagnostics on body/contactor motion instead of `joint_q` alone. | body-level path length, EE target-vs-actual error, blocking metrics |
| H4 `robot-side contactor/geometry truth is still ambiguous` | Existing c08 tool baseline already solved visible-vs-physical tool matching and same-history rerender. | Even a real physical blocker would not be meeting-defensible if geometry is fake. | Yes. Reuse the honest visible capsule/crossbar path. | Prefer the same visible tool for Stage 0. | geometry truth inventory, tool-vs-collider report, same-rollout rule |
| H5 `bridge/demo layer is truly exhausted; minimal Newton core/API fix is required` | Old limit proof says accessible actuation surfaces were exhausted, but that proof predates the current body-level clarification. | If true, Stage 0 cannot pass honestly without a minimal core fix. | Unknown until H2/H3 are resolved. | Smallest possible SemiImplicit articulation-actuation fix in `Newton/newton/`. | minimal isolated native benchmark, exact core diff report |
