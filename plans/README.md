# Plans

This directory stores execution plans for non-trivial work.

## Structure

- `active/`
  - plans currently in progress
- `completed/`
  - plans that reached a stable end state

## Rule

If a task is long-running, multi-step, or likely to span multiple sessions,
create a plan here instead of relying on chat history.
